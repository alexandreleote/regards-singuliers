{% extends 'base.html.twig' %}

{% block title %}{{ page.title }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container">
    <article class="legal-page">
        <h1>{{ page.title }}</h1>
        <div class="content">
            {{ page.content|raw }}
        </div>
    </article>
</div>
{% endblock %}
